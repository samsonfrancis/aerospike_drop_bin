This module deletes the entire bin data under a namespace, set by running a complete scan of all the existing keys under the given namespace, set.

Change the bin names prior to executing this script

